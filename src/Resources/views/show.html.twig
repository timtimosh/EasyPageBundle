{% extends 'base.html.twig' %}
{% block content %}

    <div class="jumbotron">
        <h1>{{ page.name }}</h1>
        <p class="lead">
            <img src="" alt="{{ page.name }}">
        </p>
    </div>

    <div class="row marketing">
        <div class="col-lg-12">
            {{ page.description }}
        </div>
    </div>


{% endblock %}






